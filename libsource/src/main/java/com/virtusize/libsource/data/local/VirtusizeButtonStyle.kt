package com.virtusize.libsource.data.local

/**
 * This enum contains all the styles that can be applied to the Fit Illustrator Button
 */
enum class VirtusizeButtonStyle(val value: Int) {
    DEFAULT_STYLE(0)
}